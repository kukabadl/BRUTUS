#!/bin/sh
#shopt -s nullglob
inFolder=`find . -name '*.out' -printf '%h\n' | head -n 1`
#FilesIn=(*.in)
#FilesOut=(*.out)
#NumFilesOut=${#FilesOut[@]}
#NumFilesIn=${#FilesIn[@]}

#echo labadum ${FilesIn[0]}

#if diff pub01.in pub.in >/dev/null ; then
#  echo Same
#else
#  echo Different
#fi
